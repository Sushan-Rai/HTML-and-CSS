**Task 1: Static Webpage Layout**
    - **Objective:** Create a single-page website using semantic HTML elements such as `<header>`, `<nav>`, `<main>`, and `<footer>`.
    - **Requirements:**
        - Apply basic CSS styling for typography and colors.
        - Use CSS for spacing (margins and paddings) to create a clear structure.
        - Implement a simple responsive design that adjusts the layout on smaller screens using media queries.

**F1 Page with a header nav main and footer elements**
Created a HTML skeleton with these html elements.
added CSS with margin and paddings and sections for each content i.e. welcome, about, drivers and teams.
Implemented responsive design using media queries specifically max-width and hided the contents using opacity.